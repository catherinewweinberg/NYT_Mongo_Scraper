<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">NYT Mongo Scraper</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
            {{!-- <li class="nav-item active">
                <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
            </li> --}}
            <li class="nav-item">
                <a class="btn btn-primary scrape-new" href="/api/scrape">Scrape Articles</a>
            </li>
            <li class="nav-item">
                <a class="btn btn-primary clear-new" href="/api/clear">Clear Articles</a>
            </li>
        </ul>
    </div>
</nav>
<div class="jumbotron jumbotron-fluid">
    <div class="container text-center">
        <h1 class="display-4">New York Times Mongo Scraper</h1>
    </div>
</div>
<br>
<br>
<br>


<div class="container text-left">
    <div class="row">
        <div class="col-sm-12">
            {{#each articles}}
            <div data-_id="{{this._id}}" class="card">
                <div class="card-header">
                    <h3>
                        <a class="article-link" target="_blank" rel="noopener noreferrer"
                            href="https://nytimes.com{{this.url}}">{{this.headline}}</a>
                        <a class="btn btn-danger save float-right">Save Article</a>
                    </h3>
                </div>
                <div class="card-body">{{this.summary}}</div>
            </div>
            {{/each}}
            </ul>
        </div>

    </div>
</div>